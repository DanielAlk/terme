<div class="panel panel-warning">
  <div class="panel-heading" role="tab">
    <div class="panel-heading-actions">
      <% if address.first? %>
        <%= link_to 'javascript:void(0);', class: 'link-success tooltip-trigger', title: 'Dirección principal' do %>
          <i class="fa fa-star fa-fw" aria-hidden="true"></i>
        <% end %>
      <% end %>
      <%= link_to '#body-' + address.id.to_s, class: 'link-gray', data: { toggle: 'collapse' } do %>
        <i class="fa fa-caret-down fa-fw" aria-hidden="true"></i>
      <% end %>
    </div>
    <h4 class="panel-title"><span><%= address.address %></span></h4>
  </div>
  <div id="body-<%= address.id %>" class="panel-collapse collapse" role="tabpanel">
    <div class="panel-body">
      <strong>Dirección:</strong>
      <%= address.address %>
      <br>
      <strong>Email:</strong>
      <%= address.email %>
      <br>
      <strong>Nombre:</strong>
      <%= address.fname + ' ' + address.lname %>
      <br>
      <strong>Compañía:</strong>
      <%= address.company %>
      <br>
      <strong>Código postal:</strong>
      <%= address.zip_code %>
      <br>
      <strong>Ciudad:</strong>
      <%= address.city %>
      <br>
      <strong>Provincia:</strong>
      <%= address.zone.title %>
      <br>
      <strong>Teléfono celular:</strong>
      <%= number_to_phone address.mobile %>
    </div>
  </div>
</div>