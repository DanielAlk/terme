$articles = $('<%= j render @articles %>');
$('#news-container').append($articles).find('.article-expansible').articleExpansible();
<% if @articles.next_page %>
	$('#more-news').attr('href', '<%= news_page_path(page: @articles.next_page) %>');
<% else %>
	$('#more-news').remove();
<% end %>